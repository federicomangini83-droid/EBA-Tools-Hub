const $=id=>document.getElementById(id),fileInput=$('fileInput'),processBtn=$('processBtn'),spinner=$('spinner'),errorBox=$('errorBox'),resultBox=$('resultBox'),recordCount=$('recordCount'),resultTable=$('resultTable'),downloadBtn=$('downloadBtn'),saveGithubBtn=$('saveGithubBtn'),startNameInput=$('startName'),prefixInput=$('fileNamePrefix'),endNameInput=$('endName'),fileNamePreview=$('fileNamePreview'),archiveStatus=$('archiveStatus'),refreshHistoryBtn=$('refreshHistoryBtn'),historyTable=$('historyTable'),historyError=$('historyError'),ghOwner=$('ghOwner'),ghRepo=$('ghRepo'),ghBranch=$('ghBranch'),ghFolder=$('ghFolder'),ghToken=$('ghToken'),ghRemember=$('ghRemember');
let pyodide=null,lastCSV='',lastFileName='output.csv',processing=false;const TOKEN_KEY='eba_tools_hub_github_token',POLL=3000,TIMEOUT=180000;
const sleep=ms=>new Promise(r=>setTimeout(r,ms));function cfg(){return{owner:ghOwner.value.trim(),repo:ghRepo.value.trim(),branch:ghBranch.value.trim()||'main',folder:ghFolder.value.trim().replace(/^\/+|\/+$/g,''),token:ghToken.value.trim()}}function base(c){return`https://api.github.com/repos/${c.owner}/${c.repo}/contents/${c.folder}`}function headers(c,auth=false){const h={Accept:'application/vnd.github+json','X-GitHub-Api-Version':'2022-11-28'};if(auth&&c.token)h.Authorization=`Bearer ${c.token}`;return h}function name(){return`${startNameInput.value||''}${prefixInput.value||''}${endNameInput.value||''}.csv`}function busy(v,t='Elabora file'){processing=v;spinner.hidden=!v;spinner.classList.toggle('hidden',!v);spinner.style.display=v?'inline-block':'none';processBtn.disabled=v;processBtn.textContent=v?t:'Elabora file'}function b64(s){const a=new TextEncoder().encode(s);let x='';a.forEach(b=>x+=String.fromCharCode(b));return btoa(x)}function pageUrl(c,n){return`${location.origin}/${c.repo}/${c.folder}/${encodeURIComponent(n)}`}function bytes(n){return n<1024?`${n} B`:n<1048576?`${(n/1024).toFixed(1)} KB`:`${(n/1048576).toFixed(1)} MB`}
function render(records){resultTable.innerHTML='';const hs=Object.keys(records[0]||{}),head=document.createElement('thead'),hr=document.createElement('tr');hs.forEach(h=>{const th=document.createElement('th');th.textContent=h;hr.append(th)});head.append(hr);resultTable.append(head);const body=document.createElement('tbody');records.slice(0,200).forEach(r=>{const tr=document.createElement('tr');hs.forEach(h=>{const td=document.createElement('td');td.textContent=r[h]??'';tr.append(td)});body.append(tr)});resultTable.append(body)}
const pythonReady=(async()=>{pyodide=await loadPyodide({indexURL:'https://cdn.jsdelivr.net/pyodide/v0.28.3/full/'});await pyodide.loadPackage('micropip');const m=pyodide.pyimport('micropip');try{await m.install('openpyxl')}finally{m.destroy()}const r=await fetch('./python/finrep_processor.py',{cache:'no-cache'});if(!r.ok)throw Error(`Motore Python non trovato: ${r.status}`);await pyodide.runPythonAsync(await r.text());processBtn.disabled=false})();pythonReady.catch(e=>{console.error(e);processBtn.title='Motore Python non disponibile'});
async function getFile(c,n){const r=await fetch(`${base(c)}/${encodeURIComponent(n)}?ref=${encodeURIComponent(c.branch)}&v=${Date.now()}`,{headers:headers(c,true),cache:'no-store'});if(r.status===404)return null;if(!r.ok)throw Error(`Lettura fallita: ${r.status}`);return r.json()}async function save(c,n,csv){const old=await getFile(c,n),body={message:`${old?'Aggiorna':'Aggiungi'} CSV: ${n}`,content:b64('\uFEFF'+csv),branch:c.branch};if(old?.sha)body.sha=old.sha;const r=await fetch(`${base(c)}/${encodeURIComponent(n)}`,{method:'PUT',headers:{...headers(c,true),'Content-Type':'application/json'},body:JSON.stringify(body)});if(!r.ok)throw Error(`Salvataggio fallito: ${r.status} ${await r.text()}`);return{update:!!old}}async function remove(c,i){const r=await fetch(`${base(c)}/${encodeURIComponent(i.name)}`,{method:'DELETE',headers:{...headers(c,true),'Content-Type':'application/json'},body:JSON.stringify({message:`Elimina CSV: ${i.name}`,sha:i.sha,branch:c.branch})});if(!r.ok)throw Error(`Eliminazione fallita: ${r.status} ${await r.text()}`)}async function waitDeploy(c,n,exists,csv=''){const start=Date.now();while(Date.now()-start<TIMEOUT){const r=await fetch(`${pageUrl(c,n)}?v=${Date.now()}`,{cache:'no-store'});if(!exists&&r.status===404)return;if(exists&&r.ok&&(await r.text()).replace(/^\uFEFF/,'')===csv.replace(/^\uFEFF/,''))return;await sleep(POLL)}throw Error('GitHub Pages non ha completato il deployment entro 3 minuti.')}
async function download(i){const r=await fetch(`${pageUrl(cfg(),i.name)}?v=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw Error(`Download non riuscito: ${r.status}`);const u=URL.createObjectURL(await r.blob()),a=document.createElement('a');a.href=u;a.download=i.name;document.body.append(a);a.click();a.remove();URL.revokeObjectURL(u)}
function row(i){const tr=document.createElement('tr'),n=document.createElement('td'),s=document.createElement('td'),a=document.createElement('td'),box=document.createElement('div'),dl=document.createElement('button'),del=document.createElement('button');n.textContent=i.name;s.textContent=bytes(i.size||0);box.className='row-actions';dl.textContent='Scarica';dl.className='secondary';dl.onclick=async()=>{const t=dl.textContent;dl.disabled=true;dl.textContent='Scaricamento...';try{await download(i)}catch(e){alert(e.message)}finally{dl.disabled=false;dl.textContent=t}};del.textContent='Elimina';del.className='danger';del.onclick=async()=>{const c=cfg();if(!c.token)return alert('Inserisci il token GitHub.');if(!confirm(`Eliminare ${i.name}?`))return;dl.disabled=del.disabled=true;del.textContent='Eliminazione...';tr.style.opacity='.65';try{await remove(c,i);del.textContent='Attesa deployment...';archiveStatus.textContent=`Commit creato. Attendo la cancellazione di ${i.name}...`;await waitDeploy(c,i.name,false);location.reload()}catch(e){alert(e.message);dl.disabled=del.disabled=false;del.textContent='Elimina';tr.style.opacity=''}};box.append(dl,del);a.append(box);tr.append(n,s,a);return tr}
async function history(){const body=historyTable.querySelector('tbody');body.innerHTML='';historyError.classList.add('hidden');refreshHistoryBtn.disabled=true;try{const c=cfg(),r=await fetch(`${base(c)}?ref=${encodeURIComponent(c.branch)}&v=${Date.now()}`,{headers:headers(c,!!c.token),cache:'no-store'}),items=r.status===404?[]:await r.json();if(!r.ok&&r.status!==404)throw Error(`Elenco non disponibile: ${r.status}`);items.filter(i=>i.type==='file'&&i.name.toLowerCase().endsWith('.csv')).sort((a,b)=>a.name.localeCompare(b.name)).forEach(i=>body.append(row(i)));if(!body.children.length){const tr=document.createElement('tr'),td=document.createElement('td');td.colSpan=3;td.textContent='Nessun CSV presente.';tr.append(td);body.append(tr)}}catch(e){historyError.textContent=e.message;historyError.classList.remove('hidden')}finally{refreshHistoryBtn.disabled=false}}
processBtn.onclick=async()=>{if(processing)return;errorBox.classList.add('hidden');resultBox.classList.add('hidden');const f=fileInput.files[0];if(!f){errorBox.textContent='Seleziona un file Excel.';errorBox.classList.remove('hidden');return}const p='/tmp/input.xlsx';busy(true,pyodide?'Elaborazione...':'Preparazione...');try{await pythonReady;pyodide.FS.writeFile(p,new Uint8Array(await f.arrayBuffer()));pyodide.globals.set('workbook_path_from_js',p);const r=JSON.parse(await pyodide.runPythonAsync('process_finrep(workbook_path_from_js)'));if(!r.records.length)throw Error('Nessun record valido.');lastCSV=r.csv;lastFileName=name();render(r.records);recordCount.textContent=`Record totali: ${r.count} (anteprima prime 200 righe)`;resultBox.classList.remove('hidden')}catch(e){errorBox.textContent=e.message;errorBox.classList.remove('hidden')}finally{busy(false);try{pyodide.FS.unlink(p)}catch{}try{pyodide.globals.delete('workbook_path_from_js')}catch{}}};saveGithubBtn.onclick=async()=>{const c=cfg();if(!lastCSV)return;if(!c.token)return archiveStatus.textContent='Inserisci il token GitHub.';saveGithubBtn.disabled=true;saveGithubBtn.textContent='Salvataggio...';try{const r=await save(c,lastFileName,lastCSV);saveGithubBtn.textContent='Attesa deployment...';archiveStatus.textContent=`Commit creato. Attendo la pubblicazione di ${lastFileName}...`;await waitDeploy(c,lastFileName,true,lastCSV);location.reload()}catch(e){archiveStatus.textContent=e.message;saveGithubBtn.disabled=false;saveGithubBtn.textContent='Salva nello storico GitHub'}};downloadBtn.onclick=()=>{const u=URL.createObjectURL(new Blob(['\uFEFF',lastCSV],{type:'text/csv'})),a=document.createElement('a');a.href=u;a.download=lastFileName;document.body.append(a);a.click();a.remove();URL.revokeObjectURL(u)};
[startNameInput,prefixInput,endNameInput].forEach(i=>i.oninput=()=>fileNamePreview.textContent=name());refreshHistoryBtn.onclick=history;ghRemember.onchange=()=>ghRemember.checked?localStorage.setItem(TOKEN_KEY,ghToken.value):localStorage.removeItem(TOKEN_KEY);ghToken.oninput=()=>{if(ghRemember.checked)localStorage.setItem(TOKEN_KEY,ghToken.value)};const saved=localStorage.getItem(TOKEN_KEY);if(saved){ghToken.value=saved;ghRemember.checked=true}busy(false);fileNamePreview.textContent=name();history();
